<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="hr_view_payslip_settlement" model="ir.ui.view">
        <field name="name">hr.payslip.run.tree</field>
        <field name="model">hr.payslip.run</field>
        <field name="arch" type="xml">
            <list decoration-info="state in ('confirm','hr_check','accont_check')" decoration-muted="state == 'cancel'"
                  string="Payslips">
                <field name="name"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="state"/>
                <field name="final_settlement" invisible="1"/>
            </list>
        </field>
    </record>

    <record id="hr_payslip_run_form" model="ir.ui.view">
        <field name="name">hr.payslip.run.form</field>
        <field name="model">hr.payslip.run</field>
        <field name="priority" eval="110"/>
        <field name="inherit_id" ref="hr_payroll.hr_payslip_run_form"/>
        <field name="arch" type="xml">
            <header position="inside">
                <button type="object" string="Generate Final Settlement" name="start_wizard" class="oe_highlight"
                        invisible="state != 'draft'"/>
            </header>
        </field>
    </record>
</odoo>