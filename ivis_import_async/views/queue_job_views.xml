<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_queue_job_form" model="ir.ui.view">
        <field name="name">async.import.form</field>
        <field name="model">queue.job</field>
        <field name="arch" type="xml">
            <form string="Jobs" create="false" delete="false">
                <header>
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="pending,done"
                        statusbar_colors='{"failed":"red","done":"green","cancelled":"yellow"}'
                    />
                </header>
                <sheet>
                    <group>
                        <field name="name" class="oe_inline" />
                        <field name="uuid" invisible="1"/>
                        <field name="graph_uuid" invisible="1"/>
                        <field name="func_string" invisible="1" groups="base.group_no_one" />
                        <field name="job_function_id" invisible="1"/>
                        <field name="channel" invisible="1" />
                    </group>
                    <group>
                        <group>
                            <field name="priority" />
                            <field
                                name="company_id"
                                groups="base.group_multi_company"
                            />
                            <field name="user_id" />
                            <field name="worker_pid" invisible="1" groups="base.group_no_one" />
                        </group>
                        <group>
                            <field name="date_created" />
                            <field name="date_enqueued" groups="base.group_no_one" invisible="1"/>
                            <field name="date_started" />
                            <field name="date_done" />
                            <!-- Do not use float_time as it does not work properly -->
                            <field name="exec_time" string="Time (s)" />
                        </group>
                    </group>
                    <group colspan="4">
                        <div colspan="2">
                            <label for="retry" string="Current try / max. retries" />
                            <field name="retry" class="oe_inline" /> /
                            <field name="max_retries" class="oe_inline" />
                            <br />
                            <span
                                class="oe_grey oe_inline"
                            > If the max. retries is 0, the number of retries is infinite.</span>
                        </div>
                    </group>
                    <notebook>
                      <page
                            name="results"
                            string="Results"
                            invisible="result == False and exc_info == False"
                        >
                        <group
                                name="result"
                                string="Result"
                                invisible="result == False"
                            >
                            <field nolabel="1" name="result" />
                        </group>
                        <group
                                name="exc_info"
                                string="Exception Information"
                                invisible="exc_info == False"
                                colspan="4"
                            >
                            <div id="exc_name" colspan="4">
                                <label for="exc_name" string="Exception:" />
                                <field name="exc_name" class="oe_inline" />
                            </div>
                            <field colspan="4" nolabel="1" name="exc_info" />
                        </group>
                      </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="activity_ids" widget="mail_activity" />
                    <field name="message_ids" widget="mail_thread" />
                </div>
            </form>
        </field>
    </record>

    <record id="view_queue_job_list" model="ir.ui.view">
        <field name="name">async.import.list</field>
        <field name="model">queue.job</field>
        <field name="arch" type="xml">
            <list
                create="false"
                delete="false"
                decoration-danger="state == 'failed'"
                decoration-muted="state == 'done'"
            >
                <field name="name" />

                <field name="date_created" />
                <field name="date_done" />
                <field name="exec_time" />
                <field name="exc_name" />
                <field name="exc_message" />
                <field name="state" />
                <field name="company_id" groups="base.group_multi_company" />
            </list>
        </field>
    </record>

    <record id="view_queue_job_search" model="ir.ui.view">
        <field name="name">async.import.search</field>
        <field name="model">queue.job</field>
        <field name="arch" type="xml">
            <search string="Jobs">
                <field name="name" />
                <field name="exc_name" />
                <field name="exc_message" />
                <field name="exc_info" />
                <field name="result" />
                <field
                    name="company_id"
                    groups="base.group_multi_company"
                    widget="selection"
                />
                <filter
                    name="pending"
                    string="Pending"
                    domain="[('state', '=', 'pending')]"
                />
                <filter name="done" string="Done" domain="[('state', '=', 'done')]" />
                <filter
                    name="failed"
                    string="Failed"
                    domain="[('state', '=', 'failed')]"
                />
                <filter
                    name="cancelled"
                    string="Cancelled"
                    domain="[('state', '=', 'cancelled')]"
                />
                <group expand="0" string="Group By">
                    <filter
                        name="group_by_state"
                        string="State"
                        context="{'group_by': 'state'}"
                    />
                    <filter
                        name="group_by_exc_name"
                        string="Exception"
                        context="{'group_by': 'exc_name'}"
                    />
                    <filter
                        name="group_by_exc_message"
                        string="Exception message"
                        context="{'group_by': 'exc_message'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <record id="action_queue_job" model="ir.actions.act_window">
        <field name="name">Imports</field>
        <field name="res_model">queue.job</field>
        <field name="view_mode">list,form</field>
        <field name="context">{
                               'search_default_pending': 1
            }</field>
        <field name="view_id" ref="view_queue_job_list" />
        <field name="search_view_id" ref="view_queue_job_search" />
    </record>

</odoo>
